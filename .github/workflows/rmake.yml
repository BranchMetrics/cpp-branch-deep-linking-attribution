name: rmake.bat

on: [push]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Install cmake and conan
        run: pip install --user cmake conan

      - name: Inspect workspace
        working-directory: ${{env.GITHUB_WORKSPACE}}
        run: cd; dir/w

      - name: Build Debug
        working-directory: ${{env.GITHUB_WORKSPACE}}
        run: start rmake Debug

      - name: Build Release
        working-directory: ${{env.GITHUB_WORKSPACE}}
        run: start rmake Release
